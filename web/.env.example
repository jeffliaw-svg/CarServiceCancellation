# Base URL of the RefundRoute API (the Python server).
# Leave empty for same-origin (single-host deploy, or the dev proxy).
# For a Vercel front-end with a separately hosted API, set the full URL:
#   VITE_API_URL=https://api.your-domain.com
VITE_API_URL=
